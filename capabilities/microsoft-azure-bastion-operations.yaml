naftiko: 1.0.0-alpha2
info:
  label: Azure Bastion REST API — Operations
  description: 'Azure Bastion REST API — Operations. 1 operations. Lead operation: Azure Bastion REST API List operations.
    Self-contained Naftiko capability covering one Microsoft Azure Bastion business surface.'
  tags:
  - Microsoft Azure Bastion
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_BASTION_API_KEY: MICROSOFT_AZURE_BASTION_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-bastion-operations
    baseUri: https://management.azure.com
    description: Azure Bastion REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.Network-operations
      path: /providers/Microsoft.Network/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Bastion REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_BASTION_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-bastion-operations-rest
    port: 8080
    description: REST adapter for Azure Bastion REST API — Operations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-network/operations
      name: providers-microsoft-network-operations
      description: REST surface for providers-Microsoft.Network-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Bastion REST API List operations
        call: microsoft-azure-bastion-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-bastion-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Bastion REST API — Operations. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: azure-bastion-rest-api-list
      description: Azure Bastion REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-bastion-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
